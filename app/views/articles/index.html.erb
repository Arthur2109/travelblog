
<%=render "secondary_nav"%>





<div class="container mx-auto px-4 py-4 pb-24 pt-1 mt-8">

<%=render "search_bar"%>

  <div class="grid grid-cols-1 lg:grid-cols-3 gap-16 mt-10">

    <% @articles.each do |article| %>
      <%=render partial: 'article_header', locals: {article: article}%>
    <%end%>


  </div>
  <%if signed_in? && current_user.admin?%>
    <%= link_to "create new article", new_article_path, class: "btn btn-primary md:mt-10 mt-2 mx-2" %>
  <%end%>
</div>

